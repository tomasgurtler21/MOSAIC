---
id: pre-consult-off
modes:
  - auto
pre_consult: false
---

Workflow fixture with pre_consult explicitly set to false.
Used by tests verifying that CatalogEntry.PreConsult == false when the
frontmatter declares pre_consult: false.
