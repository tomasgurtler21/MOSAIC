# Orchestration (mid-workflow)

Phase: PLANNING

Requirements and Research are complete; the single-decision test seeds this
file so its subject (the planner) starts directly from this state rather
than from an empty workspace.
