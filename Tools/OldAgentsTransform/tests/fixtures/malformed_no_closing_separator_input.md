---
id: 1
version: 1.0.0
name: bad-agent
description: Frontmatter block that is never closed (no closing --- separator)
model: {model-identifier}
tools: []

# BadAgent Agent

You are the **BadAgent** agent.

This content follows a frontmatter block that was never closed.
The transformer must detect the missing closing --- and exit non-zero.
