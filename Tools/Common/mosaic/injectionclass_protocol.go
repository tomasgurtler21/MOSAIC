package mosaic

// InjectionProtocol is the class for the CommunicationProtocol tool-managed region.
// CommunicationProtocol is regenerated on every deploy with the role-matched protocol
// block and a version marker line.
const InjectionProtocol InjectionClass = "protocol"
